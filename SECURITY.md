# Security Policy

## 취약점 신고

실제 악용 가능성이 있는 보안 취약점은 공개 Issue에 상세 내용을 게시하지 말고, 저장소 소유자 `koul777`에게 GitHub의 비공개 보안 권고(Private vulnerability reporting)가 활성화되어 있다면 해당 기능으로 알려 주세요. 비공개 신고 수단이 보이지 않으면 민감한 재현 데이터 없이 최소한의 알림만 공개 Issue에 남기고, 소유자가 비공개 연락 경로를 안내할 때까지 상세 정보를 공유하지 마세요.

신고에는 가능한 범위에서 다음 정보를 포함해 주세요.

- 영향받는 버전 또는 commit
- 취약점 유형과 예상 영향
- 민감정보를 제거한 최소 재현 절차
- 필요한 전제 조건과 실행 환경
- 제안하는 완화책이 있다면 그 내용

이 개인 오픈소스 프로젝트는 현재 정식 보안 지원 SLA, 응답 시간 또는 수정 기한을 보장하지 않습니다.

## 민감정보를 올리지 마세요

공개 Issue, Discussion, Pull Request, 테스트 fixture에 다음을 첨부하지 마세요.

- 실제 설문 원문이나 생성 보고서
- 이름, 이메일, 전화번호, 사번, 부서·직무 조합 등 개인을 식별할 수 있는 정보
- API 키, access token, Azure endpoint credential
- `out/app.db` 또는 다른 SQLite DB

필요하면 합성 데이터로 재현하고 로그·스크린샷·JSON에서 값을 먼저 삭제하세요.

## 현재 데이터 처리 원칙

- 기본 DB는 `out/app.db`이며 업로드 원본 bytes, 컬럼 프로파일, 원문을 포함할 수 있는 분석 package를 저장합니다.
- API 키는 현재 분석 요청에서만 사용하고 SQLite, package JSON, 보고서에 저장하지 않는 것을 설계 원칙으로 합니다.
- 외부 AI provider를 선택하지 않으면 설문 텍스트를 외부 서버로 자동 전송하지 않습니다.
- 외부 provider를 선택하면 마스킹된 자유응답 또는 대표 응답이 provider로 전송될 수 있습니다.
- 외부 provider와 API 키를 함께 사용하는 분석은 UI/API에서 전송 안내 확인을 요구합니다. 이 확인은 조직의 법률·보안 승인을 대신하지 않습니다.
- 마스킹은 이메일·국내 이동전화·사번/직번 등 일부 패턴만 다루며 완전한 익명화를 보장하지 않습니다.
- 이름·주소·주민등록번호·계좌번호의 명시적 문맥은 별도 검토 표시를 남기지만 자동 익명화를 보장하지 않습니다.

데이터 삭제 API를 사용할 수 있는 버전에서는 해당 dataset/run의 삭제 버튼 또는 API를 우선 사용하세요. 전체 로컬 데이터를 제거하려면 앱을 종료한 뒤 실제 `SURVEY_INSIGHT_DB_PATH`와 별도 `out*/` export 경로를 확인하고 삭제해야 합니다. DB 삭제만으로 이미 복사·공유한 보고서가 사라지지는 않습니다.

- `DELETE /model-runs/{run_id}`: 분석 run과 서버가 생성한 해당 export를 삭제합니다.
- `DELETE /datasets/{dataset_id}`: 연결 run이 있으면 기본적으로 `409`를 반환합니다.
- `DELETE /datasets/{dataset_id}?delete_runs=true`: 업로드 원본, 연결 run, 서버 export를 함께 삭제합니다.

현재 `ephemeral` 메모리 전용 저장 모드는 구현되어 있지 않습니다.
