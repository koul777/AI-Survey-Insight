[project]
name = "ai-survey-insight"
version = "0.1.0"
description = "AI-assisted Korean survey free-response analysis and report generation"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "kiwipiepy",
  "openpyxl",
  "pandas",
  "pydantic",
  "python-pptx",
  "scikit-learn",
  "uvicorn",
]

[tool.setuptools.packages.find]
include = ["survey_insight*"]
